function solution(num_list, n) {
    return num_list.slice(0,n);
    
    // for문
    // let result = []
    // for(let i = 0; i < n; i++) {
    //     result.push(num_list[i]);
    // }
    // return result;
}